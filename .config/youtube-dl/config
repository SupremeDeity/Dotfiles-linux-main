# Extract Audio
-x

# Ignore Errors
-i

# Save all videos under Movies directory in your home directory
-o ~/Music/%(title)s.%(ext)s

# Always embed thumbnails
--embed-thumbnail

# Add Artist, album etc
--add-metadata
